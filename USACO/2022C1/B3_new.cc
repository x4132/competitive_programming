#include <bits/stdc++.h>

using namespace std;

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int T;

    cin >> T;

    for (int casespadding = 0; casespadding < T; casespadding++) {
        int N, M;

        cin >> N >> M;

        unordered_map<string, int> combinations;
    
        for (int i = 0; i < M; i++) {
            cin >> combstr;

            
        }
    }
}